# ربط المشروع بـ Supabase — دليل التشغيل

المشروع بقى متصل بالكامل بمشروع Supabase (`rtfivjmqlpbqlqdxpgzh`). عشان تشغّله عندك:

## 1) قاعدة البيانات

- سكيما قاعدة البيانات (اللي بعتها) موجودة في `supabase/schema.sql` — لو سبق شغّلتها في SQL Editor بتاع Supabase مفيش داعي تكررها.
- **مهم:** السكيما الأصلية بتعمل الجداول والصلاحيات بس من غير ما تحط بيانات فعلية في `questions` و`group_round_questions` و`content_cards` — يعني من غير الخطوة الجاية، التدريب الفردي والجماعي والـ1v1 هيلاقوا الفروع فاضية من الأسئلة.
- شغّل `supabase/seed_content.sql` في SQL Editor (بعد السكيما) عشان يعمّر نفس بنك الأسئلة التجريبي (84 سؤال) اللي كان في التطبيق، ويحط عناوين ملفات لكل فرع. آمن تشغّله أكتر من مرة.
- شغّل كمان `supabase/group_training_v2.sql` — ده بيضيف جدول جديد `group_progress` (وده اللي بيخزن حالة كل سؤال في التمرين الجماعي، وعليه بيشتغل زرار الريسيت)، وبيعمّر ٥ جولات ثابتة (٢٠٠ سؤال منوّع) بنفس أسئلة الملف اللي بعتهولي، ومفعّل عليه realtime عشان صفحة الإحصائيات تتحدّث لحظيًا. آمن تشغّله أكتر من مرة (لو شغّلته قبل كده، شغّله تاني عادي).
- تقدر تستبدل أو تضيف على البيانات دي وقت ما تحب — نفس الجداول.

## 2) متغيرات البيئة

ملف `.env` اتعمل بالفعل فيه:
```
VITE_SUPABASE_URL=https://rtfivjmqlpbqlqdxpgzh.supabase.co
VITE_SUPABASE_ANON_KEY=...
```
ده الـ anon key بتاعك (مصمم يتحط في كود الفرونت إند، مش سيري — الحماية الفعلية بتبقى في RLS policies في الداتابيز).

## 3) تشغيل المشروع

المشروع بيستخدم `pnpm` (فيه `pnpm-lock.yaml`)، بس `npm` أو `yarn` هيشتغلوا برضه:

```bash
pnpm install   # أو: npm install
pnpm dev       # أو: npm run dev
```

> ملاحظة: مقدرش أنزّل الباكدجات (`@supabase/supabase-js`) هنا لأن مفيش اتصال إنترنت في البيئة اللي بشتغل فيها، فلازم تشغّل `install` عندك أول مرة.

## 4) اللي اتغيّر في المشروع

- `src/lib/supabaseClient.ts` + `src/lib/database.types.ts` — الاتصال بـ Supabase وتايبات TypeScript مطابقة للسكيما بالظبط.
- `src/lib/auth.ts` — تسجيل الدخول (id + password) + تتبّع "أونلاين" عن طريق `device_sessions`.
- `src/lib/api/*.ts` — كل التفاعل مع الجداول (subjects, solo, group, matches, stats) متقسم حسب الموضوع.
- كل الصفحات (`Login`, `Home`, `Subject`, `SoloTraining`, `GroupTraining`, `OneVsOne`, `Statistics`) بقت بتقرا وتكتب من Supabase بدل البيانات الوهمية اللي كانت في `data.ts` (اتشال).
- `src/pages/GroupTrainingPage.tsx` — اتعمل من الصفر بمنطق جديد كليًا (٥ جولات ثابتة، تنقل حر بين الأسئلة، استكمال من حيث ما وقفت، ريسيت لكل سؤال أو للجولة كلها بيمسح النقاط المرتبطة برضه) مبني على منطق الملف اللي بعته، لكن بنفس الاستايل الداكن لباقي التطبيق.
- `src/pages/MatchPlayPage.tsx` — شاشة جديدة بالكامل لمباراة الـ 1v1 المباشرة (سؤال + إجابة اللاعبين + تحكيم الحكم لحظيًا)، مكنتش موجودة في التصميم الأصلي أصلاً — زرار "ابدأ المباراة" كان من غير أي وظيفة.
- `vite.config.ts` و`index.html` كانوا بيعتمدوا على أدوات خاصة بـ Figma Make (بيستوردوا ملف `.figma/make/site.json` مش موجود في التصدير ده) — لخّيتهم لإعداد Vite عادي عشان المشروع يشتغل لوحده.

## 5) ملاحظة أمان بسيطة

الـ RLS policies في السكيما مفتوحة (`using (true)`) لأي حد معاه الـ anon key، يعني أي حد يقدر يقرا/يكتب في كل الجداول — بما فيها عمود الـ password (مخزّن كنص عادي مش مشفّر). ده مناسب لتطبيق عائلي/أصدقاء بينكم ثقة، بس لو حبيت تشدد الحماية بعدين تقدر: تشفّر الباسورد، تضيّق الـ RLS policies، أو تنقل الـ auth لنظام Supabase Auth الرسمي.
